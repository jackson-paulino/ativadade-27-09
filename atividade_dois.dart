// 2) Gerar números de 1 a 100 e pedir um número para o usuário adivinhar e dizer se o valor é maior, menor ou se acertou
class AtividadeDois {
//   var random = Random();
//   var listNum = List<Random>.generate(100, (_) => random.nextInt(100));
}